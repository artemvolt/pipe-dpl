# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
  enabled:
    - Asserts
    - \Helper\Unit
    - Db
    - Yii2:
        parts: [ orm ]
        configFile: 'config/test.sample.php'
  step_decorators: ~